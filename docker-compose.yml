version: '3.9'

services:
    vite-app:
        image: nginx:alpine
        container_name: vite-production
        ports:
            - "80:80"
        volumes:
            - ./dist:/usr/share/nginx/html
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
        restart: always
